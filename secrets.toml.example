# Copy this file's contents into Streamlit Community Cloud's Secrets box
# (App settings -> Secrets), filled in with your real Neon connection string.
# Never commit an actual secrets.toml with real credentials to GitHub —
# it's already excluded via .gitignore.

DATABASE_URL = "postgresql://user:password@host/dbname?sslmode=require"
